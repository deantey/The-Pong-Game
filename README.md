# Neon Pong 2

Upload all files and folders to GitHub.

Important:
- Keep the file name as `index.html`.
- Keep `style.css`, `game.js`, and the `sounds` folder in the same place.
- Enable GitHub Pages from Settings > Pages > Deploy from branch > main > /(root).

Test link:
https://deantey.github.io/Pong/?v=20
